# ML runtime (Keras 3 nativo — carrega .keras salvos no Colab)
# CPU-only: sem libs CUDA/cuDNN, ~250MB em vez de ~600MB
tensorflow-cpu==2.18.0
pillow==10.1.0
numpy==1.26.4
opencv-python-headless==4.8.1.78

# XAI backend (FastAPI + Grad-CAM)
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
requests==2.31.0
